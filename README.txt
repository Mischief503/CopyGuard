╔══════════════════════════════════════════════════════════════╗
║           CopyGuard v3.4.0 — Phase 17 Intelligence Logic Completion                   ║
║     AI Copy Trading Browser for Padre Terminal               ║
╚══════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  QUICK LAUNCH (you need Node.js — free at nodejs.org)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Windows:  Double-click   CopyGuard.bat
  Mac:      Right-click    CopyGuard.command → Open

  First launch downloads Electron (~50MB, ~2 min).
  Every launch after that is instant.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BUILD A STANDALONE INSTALLER (share with anyone, no Node needed)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Windows:  Double-click   BUILD.bat   → dist-build\CopyGuard Setup 1.0.0.exe
  Mac:      Run            BUILD.sh    → dist-build/CopyGuard-1.0.0.dmg

  The output is fully self-contained. Recipients double-click it,
  CopyGuard installs and runs. Nothing else required.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  FIRST LAUNCH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Click "Yes, walk me through it" — a 7-step guided tour covers
  everything you need to get live.

  Then go to ⚙ Settings and add:

    Helius API key   →  helius.dev  (free tier is enough)
    AI provider key  →  pick any one you already have:
                        Claude:     console.anthropic.com
                        GPT-4o:     platform.openai.com
                        Gemini:     aistudio.google.com
                        Grok:       console.x.ai
                        Perplexity: perplexity.ai/settings/api

  Once both keys are saved, the green dot in the sidebar lights up.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  WHAT'S IN v3.4.0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Phase 12  7-component deterministic risk engine (hard blocks)
  Phase 14  Encrypted API key storage via OS keychain
  Phase 16  Ghost Mode — paper trading before live promotion
  Phase 1   Padre trade execution fixed (sandbox bug removed)
  Phase 2   Padre bridge — execution results reported back
  Phase 3   Trusted config modal — 4 sizing modes, TP/SL, limits
  Phase 4   Promotion banner flow — Feed + Dashboard alerts
  Phase 5   First launch walkthrough — 7 steps, keyboard nav
  Phase 6   Sound alerts (7 tones) + keyboard shortcuts (10)
  Phase 7   Performance — 33 unnecessary IPC calls eliminated
  Phase 8   Ghost dashboard panel + leaderboard integration
  Phase 9   Early Bird nav badge + flow verification

  Keyboard shortcuts:
    A = approve/copy · R = reject/skip
    F/W/P/E/G/L/I = page navigation
    / = research · ? = shortcut help · Esc = close modal
